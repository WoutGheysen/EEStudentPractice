@model Oefenplatform.MVC.Areas.Teacher.Models.AssessmentFirstLang.AssessmentFirstLangDetailVm

@{
    ViewData["Title"] = "Detail";
}

<h1>Detail</h1>
<a asp-action="CreateTest">Test Maken</a>
<div>
    <h4>Assessment</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.AssessmentTitle)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.AssessmentTitle)
        </dd>
        @foreach (var item in Model.RelatedAssessments)
        {
            <dt class="col-sm-2">@item.Questions</dt>
        }
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
